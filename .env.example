# Copy this file to .env and set secure values
# Shared admin credentials for all MBA admins
ADMIN_USER=mbaadmin
ADMIN_PASS=change-me-admin
# Optional legacy API key (keeps compatibility)
API_KEY=change-me-mba-key
# Server port (optional)
PORT=8000
